---
import Base from '../../layouts/Base.astro';

// Static build — paths generated by generate-lead-site.js
export async function getStaticPaths() {
  return [];
}

const { slug } = Astro.params;
---

<Base title={`Lead — ${slug}`}>
  <main style="display:flex;align-items:center;justify-content:center;min-height:100vh;text-align:center;padding:2rem;">
    <p style="color:var(--text-muted);">Lead page: {slug}</p>
  </main>
</Base>
