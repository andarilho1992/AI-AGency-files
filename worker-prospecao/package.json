{
  "name": "andarilho-prospecao-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "logs":   "wrangler tail --format pretty",
    "secret": "wrangler secret put"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
